Generate a {{ platform }}-specific social media post using the recommended angle and hook.

Platform: {{ platform }}
{% if email_type %}
Email Type: {{ email_type }}
{% endif %}

ANGLE & HOOK CONTEXT:
- Recommended Angle: {{ angle_hook_result.recommended_angle }}
- Recommended Hook: {{ angle_hook_result.recommended_hook }}
- Rationale: {{ angle_hook_result.rationale }}
- Available Angles: {{ angle_hook_result.angles | join('; ') }}
- Available Hooks: {{ angle_hook_result.hooks | join('; ') }}

{% if brief_result %}
MARKETING BRIEF CONTEXT:
- Target Audience: {{ brief_result.target_audience | join(', ') }}
- Key Messages: {{ brief_result.key_messages | join('; ') }}
- Tone and Voice: {{ brief_result.tone_and_voice }}
- Content Strategy: {{ brief_result.content_strategy }}
{% endif %}

BLOG POST CONTEXT:
{% if input_content %}
{% if input_content.title %}
Title: {{ input_content.title }}
{% endif %}
{% if input_content.content %}
Content: {{ input_content.content }}
{% endif %}
{% endif %}

Generate a {{ platform }}-specific post that:

1. **Uses the Recommended Hook**: Start with "{{ angle_hook_result.recommended_hook }}"

2. **Applies the Recommended Angle**: Present content from the angle: "{{ angle_hook_result.recommended_angle }}"

3. **Platform-Specific Requirements**:
   {% if platform == 'linkedin' %}
   - Length: 1-3 paragraphs, 300-3000 characters
   - Include 3-5 relevant hashtags
   - Professional, thought-leadership tone
   - Engagement-focused (questions, insights, stories)
   - Clear call-to-action
   {% elif platform == 'hackernews' %}
   - Length: Concise, 500-2000 characters
   - Technical, substantive content
   - No marketing language
   - Discussion-oriented
   - Include technical details and insights
   {% elif platform == 'email' %}
   {% if email_type == 'newsletter' %}
   - Newsletter style: informative, educational
   - Well-structured, scannable format
   - Subject line: compelling, clear (50-60 characters)
   - Regular update tone
   {% else %}
   - Promotional style: CTA-focused, conversion-oriented
   - Clear value proposition
   - Subject line: action-oriented, compelling (50-60 characters)
   - Prominent call-to-action
   {% endif %}
   {% endif %}

4. **Content Requirements**:
   - Must be valuable and engaging for {{ platform }} audience
   - Must align with key messages: {{ brief_result.key_messages | join('; ') if brief_result else 'N/A' }}
   - Must match tone: {{ brief_result.tone_and_voice if brief_result else 'Platform-appropriate' }}
   - Must include a clear call-to-action

5. **Platform-Specific Elements**:
   {% if platform == 'linkedin' %}
   - Include 3-5 relevant hashtags
   - Consider mentions if appropriate
   {% elif platform == 'email' %}
   - Generate a compelling subject line (50-60 characters)
   - Structure content for email readability
   {% endif %}

Generate the complete post content now.
